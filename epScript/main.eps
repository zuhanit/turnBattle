
import gameStart as gs;
import index.indexHelper as ih;
import order.orderMain as od;
import customText as ct;
import system as sy;
import effect as ef;

function onPluginStart() {
    gs.setLight(0);
    randomize();
    EUDPlayerLoop()();
        if (getcurpl() == 0 || getcurpl() == 1 || getcurpl() == 2 || getcurpl() == 3 || getcurpl() == 4 || getcurpl() == 5) {
            if(
                Command((19), (10), 0, (215)) && 
                Command((21), (10), 0, (215))
            ){
                ct.print("\n\n\n\n\n\n\n\n\n\n");
            }
        }
    EUDEndPlayerLoop();
}


function afterTriggerExec() {
    foreach(cp : EUDLoopPlayer()) {
        setcurpl(cp);
        ih.main();
        od.main();
        gs.main();
        sy.main();
        //ef.effect(7, 8, $L("[Player 1] Move"));
   }
}