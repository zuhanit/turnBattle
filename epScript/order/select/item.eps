// 아이템 사용
import customText as ct;
var txtPtr;

function item() {
    if(
        Deaths(CurrentPlayer, Exactly, 4, $U("orderSelect"))
    ){
        txtPtr = gettextptr();;
        ct.print("\x13\x04사용할 아이템을 \x19클릭\x04해주세요!\n\x13\x04우클릭으로 명령을 취소할 수 있습니다\n\n\n\n\n\n\n");
        SetMemory(0x640B58, SetTo, txtPtr);
    }
}