// 턴 넘기기
import customText as ct;

const cp = getcurpl();
var txtPtr;

function exit() {
    if(
        Deaths(CurrentPlayer, Exactly, 5, $U("orderSelect"))
    ){
        txtPtr = gettextptr();
        ct.print("\x13\x04턴을 넘기려면 \x19클릭\x04해주세요!\n\x13\x04우클릭으로 명령을 취소할 수 있습니다\n\n\n\n\n\n\n");
        SetMemory(0x640B58, SetTo, txtPtr);
    }
}